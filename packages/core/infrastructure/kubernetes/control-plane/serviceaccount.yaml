apiVersion: v1
kind: ServiceAccount
metadata:
  name: athena-control-plane
  namespace: athena
  annotations:
    azure.workload.identity/client-id: "REPLACE_WITH_CONTROL_PLANE_MANAGED_IDENTITY_CLIENT_ID"

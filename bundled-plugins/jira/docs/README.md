# Jira Connector Pack

The bundled Jira connector pack provides fixture-backed, read-only issue context workflows.

- V1 supports issue lookup/search fixtures only.
- No Jira comments, transitions, or project writes are performed.
- Credential readiness is modeled through connector metadata, but bundled tests use fixtures and make no live network calls.

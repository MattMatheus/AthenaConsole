# Bundled Plugins

This directory is the stable repository-local search path for first-party bundled capability packs.

Packs placed here are indexed as system plugins through the normal plugin loader and manifest validation path. User-installed or workspace-local plugins continue to live under configured `ATHENA_PLUGIN_PATHS` locations such as `sample-plugins` or `.athena/plugins`.

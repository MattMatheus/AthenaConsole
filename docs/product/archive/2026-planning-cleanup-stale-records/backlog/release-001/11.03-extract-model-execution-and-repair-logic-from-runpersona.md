<!-- AUDIENCE: Internal/Technical -->

# Story: Extract 'Model Execution and Repair' Logic from runPersona

**As a backend developer, I need to extract the model interaction logic from `runPersona` into a new 'model execution' function.**

## Acceptance Criteria
*   A new function (e.g., `executeModelWithRepair`) is created.
*   It contains all logic for calling the model endpoint and the subsequent repair/retry attempts if the output is invalid.
*   `runPersona` is updated to call this new function.
*   The complex repair/retry logic is covered by new, isolated unit tests.

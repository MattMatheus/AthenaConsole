<!-- AUDIENCE: Internal/Technical -->

# Story: Build Workflow Template Instantiation UI

## Status

Completed.

## Source Decisions

- `docs/product/architecture/decisions/0008-plugin-package-format.md`
- `docs/product/architecture/decisions/0009-task-mission-run-domain-model.md`
- `docs/product/architecture/decisions/0010-sqlite-app-state-architecture.md`
- `docs/product/architecture/decisions/0011-runtime-backend-interface.md`
- `docs/product/history/completed-stories/2026.14.01-add-mission-apis.md`
- `docs/product/history/completed-stories/2026.14.02-add-workflow-template-indexing.md`
- `docs/product/history/completed-stories/2026.14.03-run-sequential-mission-plans.md`
- `docs/product/history/completed-stories/2026.16.01-instantiate-workflow-templates.md`

## User Story

As a Team Orchestrator operator, I need to instantiate a workflow template from the console so plugin-provided repeatable work can become visible missions and tasks without calling the API by hand.

## Scope

Add the first console workflow template instantiation surface using the core instantiation API.

## Acceptance Criteria

- Exposes an operator path from the workflow template catalog to instantiate an available template.
- Shows template identity, plugin identity, description, task count, and workflow goal/context preview where available.
- Presents template input fields from catalog metadata when definitions exist and honors defaults.
- Calls `POST /api/v1/workflow-templates/:id/instantiate` with supplied inputs and optional disambiguation metadata.
- Shows the created mission id, generated task ids, and direct links to existing mission/task views where available.
- Handles unavailable templates, API validation failures, and required input errors without disrupting the catalog list.
- Preserves existing catalog, mission, task, schedule, and run inspection behavior.

## Non-Goals

- No workflow-template editing.
- No workflow-template scheduling.
- No full DAG mission execution UI.
- No plugin install or remote registry UI.

## Validation Expectations

- `npm --workspace @athena/console run typecheck`
- `npm --workspace @athena/console run test`
- `npm --workspace @athena/console run lint`
- `npm --workspace @athena/console run build`
- Browser verification in Safari against local API data.
- `git diff --check`

## Engineering Handoff

- Exposed workflow template input definitions in catalog metadata so the console can render operator fields.
- Added a `workflow-templates` console feature with catalog fetching, instantiation mutation, request parsing/model helpers, and focused form-model tests.
- Rebuilt the `/workflows` console route around workflow template instantiation: catalog filtering, template preview, defaulted input fields, validation, instantiate action, and created mission/task summary.
- Added a mission-task handoff link from the created work panel to `/tasks?missionId=...`.
- Extended the task workbench page to show tasks for a `missionId` query parameter without disrupting manual task creation.
- Improved JSON/object default input rendering so object defaults show as editable JSON instead of `[object Object]`.

## QA Verdict

- Pass: `npm --workspace @athena/core run generate:schemas`
- Pass: `npm --workspace @athena/core run typecheck`
- Pass: `npm --workspace @athena/core exec vitest run tests/control-plane.workflow-template-catalog.test.ts tests/api.workflow-template-catalog.test.ts tests/api.schemas.test.ts`
- Pass: `npm --workspace @athena/core run validate:manifests`
- Pass: `npm --workspace @athena/console run typecheck`
- Pass: `npm --workspace @athena/console run test`
- Pass: `npm --workspace @athena/console run lint`
- Pass: `npm --workspace @athena/console run build`
- Pass: Safari verification at `http://127.0.0.1:5176/workflows` against seeded local API data on `127.0.0.1:8796`; instantiated a template, saw created mission/tasks, and opened `/tasks?missionId=...`.
- Pass: `git diff --check`

<!-- AUDIENCE: Internal/Technical -->

# Epic: Workflow DAG Engine

## Status

In progress. Foundation complete; executor integration remains.

## Goal

Move workflow templates from sequential mission/task projection toward durable, restart-safe, DAG-capable workflow execution.

The target operator experience is:

- instantiate or schedule a workflow template,
- receive one canonical workflow DAG run id,
- inspect graph status for real task execution progress,
- recover safely after restart,
- and preserve existing mission/task compatibility while the DAG executor matures.

## Source Decisions

- `docs/product/architecture/decisions/0008-plugin-package-format.md`
- `docs/product/architecture/decisions/0009-task-mission-run-domain-model.md`
- `docs/product/architecture/decisions/0011-runtime-backend-interface.md`
- `docs/product/architecture/decisions/0012-event-artifact-observability-model.md`
- `docs/product/architecture/decisions/0015-canonical-orchestration-state-model.md`

## Why Now

Team Orchestrator can create tasks, group them into missions, instantiate workflow templates, schedule templates, and inspect run history. The durable DAG substrate now exists, but workflow-template execution still mostly follows the mission/task projection path.

The remaining product gap is execution linkage: real task runs need to update workflow DAG steps, schedules need to drive the canonical DAG run, and console surfaces need to inspect actual workflow-template runs rather than seeded DAG state.

## Progress Tracker

### 2026.17.01 Implement Workflow DAG Definition Parser

Status: Completed.

Artifact:

- `docs/product/history/completed-stories/2026.17.01-implement-workflow-dag-definition-parser.md`

Delivered:

- Parses explicit task dependencies.
- Validates duplicate ids, missing references, self-dependencies, and cycles.
- Produces deterministic topological order and dependency maps.
- Surfaces validation errors through manifest validation/indexing.
- Preserves sequential templates without explicit dependencies.

### 2026.17.02 Implement Workflow State Store and Resumption Logic

Status: Completed.

Artifact:

- `flywheel/backlog/engineering/done/STORY-20260528-workflow-state-store-resumption.md`

Delivered:

- Durable workflow DAG run, step, and event state.
- Step attempts, timestamps, readiness, failure, output, and blocking dependency state.
- Service helpers for create/load/update/resume.
- Stale running step recovery after restart.

### 2026.17.03 Add Visualizer-Friendly Workflow Status API

Status: Completed.

Artifact:

- `flywheel/backlog/engineering/done/STORY-20260528-workflow-status-api.md`

Delivered:

- Graph-friendly workflow run status API.
- Nodes, edges, progress, events, recovery metadata, polling interval, and etag.
- API coverage for console polling.

### 2026.17.04 Create Workflow DAG Run Envelope For Workflow Templates

Status: Completed.

Artifact:

- `flywheel/backlog/engineering/done/STORY-20260528-workflow-template-dag-run-envelope.md`

Delivered:

- Workflow-template instantiation creates a canonical workflow DAG run.
- Instantiation result exposes `workflowDagRun.id` additively.
- Workflow-template schedule execution records `workflowDagRunId` in schedule history.
- Mission context and task provenance link back to workflow DAG run and step ids.

### 2026.17.05 Link Workflow DAG Steps To Task Run Outcomes

Status: Active.

Flywheel item:

- `flywheel/backlog/engineering/active/STORY-20260528-workflow-dag-step-task-run-linking.md`

Purpose:

- Keep existing task/mission execution behavior while using task provenance to update workflow DAG steps with real task run lifecycle data.

Acceptance summary:

- Task runs created from workflow-template tasks start/complete/fail the matching DAG step.
- DAG step output/failure includes task run identifiers and useful execution detail.
- Workflow status for an instantiated template reflects real task run outcomes.

### 2026.17.06 Add Deterministic Workflow DAG Executor Service

Status: Planned.

Flywheel item:

- `flywheel/backlog/engineering/intake/STORY-20260528-workflow-dag-executor-service.md`

Purpose:

- Introduce a service that executes workflow DAG runs by dependency readiness instead of relying only on mission task order.

Acceptance summary:

- Executor selects ready pending steps, runs their projected tasks, and recomputes readiness.
- First implementation may run one step at a time for determinism.
- Existing mission/task APIs remain compatible.

### 2026.17.07 Resume Workflow DAG Runs After Restart

Status: Planned.

Flywheel item:

- `flywheel/backlog/engineering/intake/STORY-20260528-workflow-dag-restart-resume.md`

Purpose:

- Make interrupted workflow DAG runs recoverable and resumable from durable state.

Acceptance summary:

- Startup recovery marks stale running steps resumable with events.
- Resume restarts from failed/stale steps without re-running completed dependencies.
- Status API exposes the resumed state accurately.

### 2026.17.08 Run Scheduled Workflow Templates Through DAG Execution

Status: Planned.

Flywheel item:

- `flywheel/backlog/engineering/intake/STORY-20260528-workflow-template-schedule-dag-execution.md`

Purpose:

- Move scheduled workflow-template attempts from “instantiate projection only” toward canonical DAG execution.

Acceptance summary:

- Due workflow-template schedules create or reuse the canonical DAG run and execute through the DAG executor path.
- Schedule history records workflow DAG run id, mission id, task ids, terminal status, and errors.
- Existing schedule APIs remain backward compatible.

### 2026.17.09 Add Console Workflow Run Graph Inspection

Status: Planned.

Flywheel item:

- `flywheel/backlog/engineering/intake/STORY-20260528-workflow-run-graph-console.md`

Purpose:

- Let operators inspect real workflow-template DAG runs from the console.

Acceptance summary:

- Console links from workflow-template instantiation/schedule history to workflow DAG run status.
- Graph/list view shows step readiness, status, failures, and recovery affordances.
- UI handles polling and terminal states without obscuring existing mission/task details.

### 2026.17.10 Reconcile Legacy Workflow APIs With Canonical DAG Runs

Status: Planned.

Flywheel item:

- `flywheel/backlog/engineering/intake/STORY-20260528-legacy-workflow-dag-alignment.md`

Purpose:

- Clarify or retire legacy file-backed workflow run APIs so the product has one understandable workflow execution model.

Acceptance summary:

- Legacy workflow routes are labeled, bridged, or deprecated against canonical DAG runs.
- Docs and API behavior make the canonical workflow DAG run model explicit.
- No ambiguous “workflow run” surfaces remain for operators.

## Suggested Story Sequence

1. `STORY-20260528-workflow-dag-step-task-run-linking`
2. `STORY-20260528-workflow-dag-executor-service`
3. `STORY-20260528-workflow-dag-restart-resume`
4. `STORY-20260528-workflow-template-schedule-dag-execution`
5. `STORY-20260528-workflow-run-graph-console`
6. `STORY-20260528-legacy-workflow-dag-alignment`

## Tracking Notes

- Stories should stay additive until the DAG executor has proven parity with existing mission/task behavior.
- Prefer compatibility fields and links over response-shape churn.
- Each implementation story should include at least one integration test that starts from a workflow-template instantiation or schedule, not only hand-seeded workflow DAG state.

## Non-Goals

- No hosted scheduler.
- No visual workflow editor yet.
- No automatic planner that invents tasks from natural language.
- No broad replacement of existing mission/task behavior until workflow DAG execution proves out.
- No file-backed state migration outside the workflow execution surfaces named in this epic.

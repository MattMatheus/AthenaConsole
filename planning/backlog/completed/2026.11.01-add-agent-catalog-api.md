<!-- AUDIENCE: Internal/Technical -->

# Story: Add Agent Catalog API

## Status

Completed.

## Source Decisions

- `planning/architecture/0007-agent-manifest-and-lifecycle-contract.md`
- `planning/architecture/0008-plugin-package-format.md`
- `planning/architecture/0010-sqlite-app-state-architecture.md`
- `planning/backlog/refinement/2026.11.00-epic-local-agent-catalog.md`

## User Story

As a Team Orchestrator operator, I need an API/service surface for indexed plugins and agents so the console can show what local agents are installed, valid, and available for tasks.

## Scope

Expose the local plugin and agent index through core services and API routes suitable for the console.

## Acceptance Criteria

- Lists indexed plugins and agents from SQLite app state.
- Filters agents by capability.
- Returns manifest-derived metadata needed by the console catalog.
- Returns plugin and agent validation/load errors.
- Distinguishes system and workspace plugin sources.
- Includes focused tests for service behavior, API response shape, filtering, and error visibility.
- Keeps implementation aligned with local-first defaults.

## Non-Goals

- No console UI page.
- No plugin install/registry flow.
- No remote plugin source.
- No task creation flow.
- No agent execution changes.

## Validation Expectations

- `npm --workspace @athena/core run typecheck`
- Focused core tests for the new catalog API/service behavior.
- `npm --workspace @athena/core run test:unit` if shared API or control-plane surfaces are touched broadly.

## Engineering Handoff

Implemented the local agent catalog service and API surface over SQLite app state.

Delivered:

- Added shared catalog contracts for plugin summaries, agent summaries, validation issues, source scope, and capability filters.
- Added `LocalAgentCatalogService` over `plugin_index` and `agent_index`.
- Added `GET /api/v1/agent-catalog/plugins`.
- Added `GET /api/v1/agent-catalog/agents` with `capability` and `capabilities` query filtering.
- Returned manifest-derived console metadata for plugins and agents, including implementation, limits, observability, permissions, UI metadata, and compatibility fields when present.
- Returned plugin validation/load errors with inferred `plugin`, `agent`, or `unknown` resource type.
- Preserved the indexed DB `sourceType` while exposing operator-facing `sourceScope` values of `workspace` or `system`.
- Registered API response schemas and route-family metadata.
- Restored the `packages/core/AGENTS.md` Stage 8 marker expected by the existing docs consistency test.

Primary files:

- `packages/core/src/shared/contracts/agent-catalog.ts`
- `packages/core/src/control-plane/services/agent-catalog.ts`
- `packages/core/src/api/routes/agent-catalog-routes.ts`
- `packages/core/tests/control-plane.agent-catalog.test.ts`

## QA Verdict

Passed.

Validation:

- Pass: `npm --workspace @athena/core run typecheck`
- Pass: `npx vitest run tests/control-plane.agent-catalog.test.ts tests/api.route-registration.test.ts tests/api.schemas.test.ts tests/api.server.test.ts`
- Pass: `npx vitest run tests/control-plane.api-contracts.test.ts tests/docs.stage-consistency.test.ts`
- Pass: `npm --workspace @athena/core run test:unit`
- Pass: `git diff --check`

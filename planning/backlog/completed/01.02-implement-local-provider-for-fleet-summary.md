# Story: Implement Local Provider for Fleet Summary

**As a developer, I want the existing local file-based metrics provider to correctly implement the `FleetSummary` contract so that it serves as the reference implementation.**

## Status
Completed on 2026-02-17.

## Acceptance Criteria
*   The local metrics provider implements the new `FleetSummary` interface.
*   It correctly populates the agent counts by reading the local state.
*   It leaves capability-dependent fields (like CPU/memory) as `null` or `undefined`.
*   Existing tests for the local provider are updated and pass.

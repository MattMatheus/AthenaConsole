# Story: Deprecate and Remove Monolithic Router Function

**As a backend developer, I need to remove the old `routeRequest` function after all its logic has been migrated to the new `APIRouter`, completing the refactor.**

## Acceptance Criteria
*   All logic from the original monolithic router function has been moved to new-style handlers.
*   The server's main entry point now calls the `APIRouter.dispatch()` method instead of the old function.
*   The old `routeRequest` function and any related dead code are deleted.
*   All API integration tests pass, confirming no regressions.

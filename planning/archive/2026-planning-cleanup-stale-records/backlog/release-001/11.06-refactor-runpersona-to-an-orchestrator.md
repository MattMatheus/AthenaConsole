<!-- AUDIENCE: Internal/Technical -->

# Story: Refactor runPersona to an Orchestrator

**As a backend developer, I need to refactor the `runPersona` function to be a simple orchestrator that calls the newly extracted helper functions, to improve its readability and maintainability.**

## Acceptance Criteria
*   The body of the `runPersona` function is replaced with a clean, readable sequence of calls to the new helper functions (preflight, prompt construction, execution, etc.).
*   The external contract (arguments and return type) of `runPersona` remains unchanged.
*   All existing integration tests for `runPersona` continue to pass, proving no change in behavior.
*   The overall complexity of the `runPersona` function itself is significantly reduced.
